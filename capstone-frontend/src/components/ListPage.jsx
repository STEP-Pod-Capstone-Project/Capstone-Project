import React, { Component } from 'react';

class ListPage extends Component {
  render() {
    return (
      <div> render a list's info- picture, owner, title, books, etc </div>
    );
  }
}

export default ListPage;